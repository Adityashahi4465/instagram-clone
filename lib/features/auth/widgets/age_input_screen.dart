

import 'package:flutter/material.dart';

class AgeInputScreen extends StatelessWidget {
  const AgeInputScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}